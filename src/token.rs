#[derive(Debug)]
pub enum Token {
    PrimitiveValue,
    NegativValue,
    PositiveValue,
    Zero,
    One,
    StructStart,
    StructEnd,
    ArrayStart,
    ArrayEnd,
    Attribut_red,
    Attribut_green,
    Attribut_blue,
    Attribut_x,
    Attribut_y,
    Attribut_position,
    Attribut_rotation,
    Attribut_width,
    Attribut_border_color,
    Attribut_fill_color,
    Attribut_vertices,
    Attribut_visible_extent,
    Attribut_background_color,
    Attribut_shapes,
}
